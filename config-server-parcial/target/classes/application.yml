server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server-parcial
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Elkinssm/parcial-backend-henry-gutierrez.git
          default-label: main
          force-pull: true
          search-paths: configuration-parcial
